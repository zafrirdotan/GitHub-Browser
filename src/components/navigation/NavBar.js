import React from 'react';

import './NavBar.scss';

const NavBar = props => (
  <header className="app-header">
    <div className="app-title">GitHub Browser</div>
  </header>
);
export default NavBar;
